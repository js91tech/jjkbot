DISCORD_TOKEN=your_bot_token
DISCORD_CLIENT_ID=your_application_id
DISCORD_CLIENT_SECRET=your_oauth_secret
# Server ID for instant slash commands (recommended). Leave empty for global (slow).
DISCORD_GUILD_ID=
# Bot auto-registers slash commands on start (set false to disable)
REGISTER_COMMANDS_ON_START=true

DATABASE_PATH=./data/jjk.db
WEB_PORT=3847
# Top-down Phaser client URL (jjk-game-2d). Local: http://localhost:5173 — prod: your hosted dist URL
GAME_2D_URL=http://localhost:5173
# Optional: public API URL shown on 2D launcher page (defaults to http://localhost:3848)
API_PUBLIC_URL=http://localhost:3848
# Must match Discord OAuth2 redirect exactly (https, no trailing slash)
WEB_BASE_URL=http://localhost:3847
# On Railway you can omit WEB_BASE_URL if RAILWAY_PUBLIC_DOMAIN is set; otherwise set:
# WEB_BASE_URL=https://your-service.up.railway.app
SESSION_SECRET=change_me_to_random_string

# Optional: comma-separated Discord user IDs with admin powers
ADMIN_DISCORD_IDS=

# --- 2D Discord Activity (SERVICE=api + jjk-game-2d on Railway) ---
# See docs/RAILWAY-2D-ACTIVITY.md
# API service:
#   SERVICE=api
#   ACTIVITY_ORIGINS=https://your-2d-app.up.railway.app
#   ALLOW_DEV_AUTH=false
# Web service (optional dashboard link):
#   GAME_2D_URL=https://your-2d-app.up.railway.app
#   API_PUBLIC_URL=https://your-api.up.railway.app
