# Web service — Railway config-as-code
# Deploy → clear "Custom Start Command" in UI if deploy still fails (let this file control it).

[deploy]
startCommand = "npm run start -w @jjk/web"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
